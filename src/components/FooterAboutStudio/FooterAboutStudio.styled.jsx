import styled from "styled-components";
import { DefaultTextColor } from "../../utils/Colors";

export const StyledWrapper = styled.div`
    // display: inline-flex;
    // flex-direction: column;
    row-gap: 16px;
`;

export const StyledText = styled.p`
    font-size: 16px;
    color: ${DefaultTextColor};
    margin-top: 10px;
    }



`